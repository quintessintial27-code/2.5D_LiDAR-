# ─────────────────────────────────────────────────────────────────────────────
# Adaptive Variable Resolution 2.5D LiDAR Mapping — CPU-Only Requirements
# Tested on Python 3.10 | Intel Celeron | 8 GB RAM | Windows 10/11
# ─────────────────────────────────────────────────────────────────────────────

# Core deep learning (CPU build — no CUDA needed)
# Install via: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Training framework
lightning==2.1.3

# Config management
hydra-core==1.3.2
omegaconf==2.3.0

# Point cloud & numerical processing
numpy==1.24.4
scipy==1.11.4
scikit-learn==1.3.2

# 3D visualization
open3d==0.18.0

# Plotting & metrics
matplotlib==3.7.4
seaborn==0.13.0

# Utilities
tqdm==4.66.1
Pillow==10.1.0
pyyaml==6.0.1
pandas==2.1.4
einops==0.7.0

# KITTI evaluation
# (built-in, no extra package needed — we implement it)

# Testing
pytest==7.4.3
pytest-cov==4.1.0
